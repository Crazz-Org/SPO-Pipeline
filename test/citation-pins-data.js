'use strict';
// PINNED_CITATIONS data -- action 11.1 (#206), fix pass D1-D8 applied; action 11.2 (#206) added
// a third. Three registries, all consumed by test/doc-constant-sweep.test.js through
// test/citation-pins.js's resolvePins.
// Generated by reading the real target files (not hand-typed): every `first`/`last` string here
// is the exact, trimmed-at-compare-time text of the line the citation's own repo held at the
// pin's commit, read once during this action and never re-derived from EXPECTED_CITATIONS or from
// the doc itself (a consistent wrong re-pin would move both of those together and still look
// right -- see the doc header this action added to both bench docs, and #206's own probe 1).
//
// BENCH_PINS -- doc/bench-audit-2026-09-02.md and doc/bench-plan-derived-2026-09-02.md, now IN
// the anchor-checked corpus (removed from ANCHOR_EXCLUDED_FILES): 41 file-tied citations, one pin
// each (the 2 `sanctuarize.test.ts:151-156` dangling citations stay CITATION_ALLOWLIST-only, per
// this action's own spec -- a deleted file has no line to pin against).
//   - SPO-Pipeline-rooted citations are frozen at 7902164309c1766d7b785daab9ba94ff6472bc1d (short
//     `7902164`) -- the commit doc-constant-sweep.test.js's own reference measurement used for
//     BOTH docs; confirmed by diffing 7902164..HEAD for both files -- the only citations that
//     moved since are the two kept live below. Full 40-char sha per the fix pass's own D6/D8b
//     finding: a 7-char sha can become ambiguous as the repo grows.
//   - SPO-WebClient-rooted citations are frozen at
//     935283890fa0593c5c5d0b41cceeaec2c1972c6f (short `9352838`), NOT the `d03ea8b7` this action
//     first shipped with -- fix pass D6 (verifier finding): the remediation plan's own row 1.2
//     records `~/SPO-WebClient` was on `feat/suggestion-report-kind`, tip `93528389`, when the
//     8.1 audit was measured. Verified: `93528389` is an ancestor of `origin/main` (`git
//     merge-base --is-ancestor 93528389 origin/main`), so it is a durable, reachable commit, not
//     a deletable branch tip -- case 1 of D6's own decision tree. Diffed against `d03ea8b7`: only
//     `scripts/board-take.sh` differs (2 net lines added above the cited block); every other
//     product file this doc set cites is byte-identical at both commits, so only board-take.sh's
//     pin needed to change when the base commit did.
//   - Two facts are kept LIVE (`at: 'HEAD'`) because they are hand-maintained as true today, not
//     dated record: the `bin/spo` `collectAll` call site (re-pinned 15+ times as `bin/spo` grows;
//     `binSpoLineNamesCollectAll` already covered this one, this pin formalizes it in the shared
//     mechanism) and `doc/state-machine-spec.md`'s FINISH row.
//   - Corrections verified by reading `git show <sha>:<path>` at the CORRECT base commit above
//     (935283890fa0...  / 7902164309c...):
//       - `worker.ts:301` -> `:302` (both docs): `request.fingerprint.head`'s own `const head =`
//         line, not the comment above it. True at BOTH `d03ea8b7` and `93528389` -- this one was a
//         real off-by-one in the original citation, not an artifact of picking the wrong base.
//       - `board-take.sh:109-110` -- KEPT AS ORIGINALLY WRITTEN (fix pass D6 reverted this
//         action's own first-pass ":111-112" correction): at `93528389`, `:109-110` is exactly
//         the `finished_marker=...` / `if [ -n "$finished_marker" ]...` pair that reads
//         `.finished` -- the ":111-112" figure was only correct against `d03ea8b7`, the wrong base
//         commit, and would have rewritten a citation that was already true when the record was
//         written.
//       - `scripted.js:1944-1994` -> `:1944-1996` (bench-audit only): `realFinish` genuinely ends
//         at `:1996` (`}` closes the function); `:1994` is a blank line INSIDE the function, two
//         lines short of its real end (fix pass D7).
//       - `test/helpers.js:65-80` -> `:65-94` (both docs): `isolatedEnv` spans `:65-94`; the
//         prose's own claim (the model example that sets `SPO_BENCH_DIR` to a temp dir) is the
//         `SPO_BENCH_DIR: mkTmp(...)` line at `:92`, seven lines past where the old citation
//         stopped (fix pass D7). Every OTHER frozen range in this registry was re-checked the same
//         way (does `last` actually end the construct/claim the prose describes) and found
//         correct; see the fix pass's own report for the full list checked.
//
// LIVE_RANGE_PINS / BLUNT_PINS -- every RANGE citation in the live (non-bench) anchor corpus --
// anchored, unanchorable, or on CITATION_ANCHOR_ALLOWLIST -- plus the 3 ANCHOR_BLUNT_CITATIONS
// single-line entries, each given a literal-text pin alongside (or, for the allowlisted ones,
// INSTEAD of) the identifier-based anchor check: a pin discriminates a +/-1 drift on a RANGE,
// which the anchor check cannot by construction (doc-constant-sweep.test.js part 2.5's own
// header). 14 range pins, not 11 -- fix pass D2/R1 added the three this action's first pass
// missed:
//   - `orchestrator/park-loop.js :: intake.js:953-955` -- the 4th citing file of the same
//     Fable-incident fact (state-machine-spec.md, README.md and steps/llm.js were already pinned;
//     park-loop.js was not, for no stated reason -- an oversight in the first pass's own data
//     collection).
//   - `orchestrator/README.md :: dispatcher.js:635-648` -- on CITATION_ANCHOR_ALLOWLIST, so it
//     never entered the corpus-wide walk this registry was first built from; a consistent +/-1
//     re-pin here was probed and confirmed to stay green without this pin. Re-pinned :634-648 ->
//     :635-648 in the SAME fix pass action that corrected doc/state-machine-spec.md's sibling
//     citation to the identical fact (D3): :634 is a blank line, :635 is the block's own `if`.
//   - `orchestrator/README.md :: SPO-WebClient/.claude/settings.json:109-127` -- fix pass D2
//     first found this UNPINNABLE (measured: THIS repo's own `.claude/settings.json` has exactly
//     one commit, already matching HEAD, never 127 lines) and named it on a `RANGE_PIN_UNAVAILABLE`
//     exception. That was the wrong diagnosis, not an unpinnable fact: fix pass R1 found the
//     citation is TRUE, it just names the OTHER repo -- issue-429's PLAN ran with cwd in an
//     SPO-WebClient worktree (base `de2039e9`), and THAT repo's `.claude/settings.json:109-127`
//     is exactly the `"hooks": {` block through the third `PreToolUse` hook's `"timeout": 10`
//     line, byte-identical at `de2039e9`, `93528389` and HEAD. Re-spelled in orchestrator/README.md
//     with the `SPO-WebClient/` prefix (so resolveCitationTarget routes it to the product repo)
//     and pinned here frozen at the same `93528389` sha every other product citation uses --
//     `RANGE_PIN_UNAVAILABLE` and its exception are retired, not merely emptied.
const BENCH_PINS = [
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:482", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      const code = await deps.runCommand('node', ['scripts/verify-gate.js', '--live', ...args], {" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:482-486", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      const code = await deps.runCommand('node', ['scripts/verify-gate.js', '--live', ...args], {", last: "      });" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "src/e2e/bench/worker.ts:482", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      const code = await deps.runCommand('node', ['scripts/verify-gate.js', '--live', ...args], {" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "verdict.ts:23-67", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "export interface BenchVerdict {", last: "}" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:307-319", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    writeVerdictIn(deps.paths.verdicts, {", last: "    });" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "verdict.ts:162-183", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "export function statusDescription(verdict: BenchVerdict): string {", last: "}" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "scripted.js:1347", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  if (r.exit === 0) return 'CI_CHECKS';" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:495-502", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    } else if (request.type === 'live' || request.type === 'nightly') {", last: "      bodyDetail = `live drive exited ${code} (${bodyVerdict})`;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "src/e2e/bench/paths.ts:143-163", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "export function workerStatus(", last: "}" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "merge-queue.ts:178-188", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "export function mayReuseVerdict(", last: "}" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "cli.ts:179", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    type === 'ref' ? { head: ref as string, hash: `ref:${ref}`, clean: true } : deps.fingerprint(worktree);" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:543-546", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      report.fingerprints.atEnd !== undefined &&", last: "    report.targetMoved = !stable;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:302", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    const head = request.fingerprint.head;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "run.ts:109", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  const failed = releaseError !== undefined || results.some(r => r.status === 'FAIL');" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:487", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      bodyVerdict = GATE_EXIT_VERDICT[code] ?? 'FAIL';" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "cli.ts:221-227", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "async function wait(id: string, timeoutMinutes: number, deps: CliDeps): Promise<number> {", last: "      return report.verdict === 'PASS' || report.verdict === 'LEASED' ? 0 : 1;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "scripts/nightly-check.sh:70-73", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  ENVIRONMENT|INTERRUPTED)", last: "    ;;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "scripted.js:292-293", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  const nightly = readJsonSafe(path.join(config.spoBenchDir, 'nightly', 'latest.json'));", last: "  if (nightly && nightly.verdict === 'FAIL' && nightly.sha === originMainSha) {" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:106", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "const DONE_RETENTION_MS = 24 * 60 * 60 * 1000;" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:576", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "      deps.spool.purgeDone(DONE_RETENTION_MS, deps.now());" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "src/e2e/config.ts:93", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  minIntervalMinutes: numberFromEnv('E2E_MIN_INTERVAL_MINUTES', 0)," },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:750", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  const beat = setInterval(() => touchHeartbeat(paths), HEARTBEAT_PERIOD_MS);" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "bin/spo:1283", at: "HEAD", first: "    const data = collectAll(sources);" }, // re-pinned from :1273, then :1276 -- card #219's fix pass expanded the injected-deps comment above cmdStatus's computeDispatcherStatus call (monotonicNowMs require plus TWO option lines: processStartUptimeMs and monotonicNowMs: monotonicNowMs()), net +7 lines above cmdDashboard's own collectAll call, a true pure shift; content byte-identical at :1283
  { file: "doc/bench-audit-2026-09-02.md", citation: "finish.sh:275-276", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    mkdir -p \"$SESSIONS_DIR\"", last: "    printf '%s\\t%s\\t%s\\n' \"$here\" \"$branch\" \"$(git -C \"$here\" rev-parse HEAD)\" > \"$SESSIONS_DIR/$(session_key \"$here\").finished\"" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "board-take.sh:109-110", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  finished_marker=\"$(session_marker finished 2>/dev/null || true)\"", last: "  if [ -n \"$finished_marker\" ] && [ -f \"$finished_marker\" ]; then" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "doc/state-machine-spec.md:207", at: "HEAD", first: "| FINISH | script | Action B1.4: FINISH now actually keeps the promise this row always made — fast-forward the main product checkout (`config.productRepo`), then reap the worktree, then close the task (board sync: Done + short comment). Before this action it never did: `realFinish` only ever did the board move, the issue comment and the worktree remove, so a PR merged by the daemon left `config.productRepo` exactly as stale as one merged by a human who never ran `npm run finish` (SPO-WebClient's own script) — measured root cause of the bench worker silently running a stale binary for 3.5 days across 11 merges (a commit changed both halves of a flag contract atomically and correctly; the bench worker, installed once from a binary and never rebuilt, disagreed with the new job body from the moment it landed). Two NEW steps run first, inside their own product-repo-lock critical section (phase `finish-sync`, ahead of the pre-existing teardown phase `finish` below — see product-repo-lock.js/product-repo-hold.js), in this order: (1) `git fetch origin`, then this card's own merge commit by PR number (`gh pr view <prNumber> --json mergeCommit` — ctx carries no merge sha directly; MERGE only ever enqueues and awaits the merge, never reads the resulting commit back), then `git diff --name-only <mergeSha>^ <mergeSha>` to learn whether the merge touched the bench worker's own sources (paths under `src/e2e/bench/` or `scripts/bench-`, the same test `scripts/finish.sh` already runs by hand) — this determines `benchTouched` REGARDLESS of whether the fast-forward below succeeds; (2) the fast-forward itself — refuse (never force) unless `config.productRepo` is on `main`, clean of TRACKED changes (`git status --porcelain --untracked-files=no` empty — post-verification hazard fix: narrowed from bare `--porcelain`, which counted untracked files and so refused in cases `scripts/finish.sh`'s own `git pull --ff-only` would sail straight through, parking a bench-touching card on a stray editor backup or scratch file in this human-shared checkout), and `git merge --ff-only origin/main` itself succeeds; (3) ONLY once the fast-forward succeeded AND `benchTouched` is true, wait for the bench worker to go IDLE — a second post-verification hazard fix: `bash scripts/bench-install.sh` ends in an unconditional `systemctl --user restart`, and this daemon runs `SPO_WORKERS=2` in production, so reinstalling while the bench is still busy can cut a SIBLING card's in-flight GATE mid-job (the cut job recovers as `INTERRUPTED`, writes no `verdicts/<sha>.json`, and that sibling parks `gate-non-attesting` — transient-retryable, but a REAL re-run of WORKTREE through GATE, not merely a wasted gate). `waitForBenchIdle` polls `~/.spo-bench/spool` and `~/.spo-bench/running` (the same two directories `spo status` already reports) until both are empty, bounded by `config.benchIdleWaitMaxPolls` × `config.benchIdleWaitPollIntervalMs` (default 180 × 5s = 15 minutes), reading `config.spoBenchDir` itself, never a hardcoded path (R2/W2, post-verification third pass: an UNREADABLE spool/running — anything other than \"the directory simply is not there\" — is never silently read as idle; it PARKS immediately, `finish-failed`/`bench-idle-wait`/`bench-dir-unreadable`, distinguishably from a merely busy bench, the same \"tell a misconfiguration apart from a genuine empty answer\" pattern `realGate`'s own `verdictDirExists` already uses one function away). R1 (post-verification third pass): a bench that stays BUSY for the whole bound no longer PARKS — it DEFERS. The old park was wrong on three counts, all measured: `finish-failed` is not on `state-machine.js`'s `TRANSIENT_RETRY_REASONS` (terminal, human-only); the park fired BEFORE the board move below, so a card whose PR had ALREADY MERGED sat in `Merging` with its worktree still on disk; and the 15-minute bound is not generous against SPO-WebClient's own bench leases — `worker.ts`'s own `DEFAULT_LEASE_MINUTES = 30` / `MAX_LEASE_MINUTES = 120` constants mean an ORDINARY human bench lease (2×–8× the bound) would terminally park any bench-touching card the daemon finished during it. So the bound being exhausted now journals `bench-reinstall-deferred` (`detail`/event carry `mergeSha`, `prNumber`, the last-observed `spool`/`running`, and the attempt count), records the debt DURABLY (`journal.js`'s `writeBenchReinstallOwed`, `<journalRoot>/bench-reinstall-owed.json` — survives a daemon restart, and a SECOND bench-touching card deferring during the same busy window overwrites this ONE record with its own, newer `mergeSha` rather than accumulating a duplicate — `bash scripts/bench-install.sh` always rebuilds from whatever is CURRENTLY checked out, so only the latest sha is ever useful to retry with), and lets FINISH complete NORMALLY — board move, comment, worktree remove, `DONE` — exactly as if nothing were owed. Round 4: the debt is paid back by the NEXT card's own WORKTREE, from inside that card's own product-repo lock span (`payBenchReinstallDebtIfOwed`, see the WORKTREE row above for the full mechanism) rather than a separate daemon scan timer — round 3's `orchestrator/bench-reconcile.js` shipped exactly that timer and was deleted: it held the SAME product-repo lock from a THIRD process the mutex's own wait-bound derivation (`product-repo-lock.js`'s `waitBoundMs`) assumes cannot exist. The journal alone must always answer \"is a reinstall owed right now\" — every branch above is named and loud on purpose; (4) only once the bench is confirmed idle, `bash scripts/bench-install.sh` (the same script `scripts/finish.sh`'s human-session rule runs) reinstalls the worker — never run against a checkout that could not be verified fresh first, which would install the wrong binary and report success, reproducing the exact defect this action closes, and never run while the bench is still busy, which would reproduce the SAME defect from the other direction (a cut sibling job). | DONE | PARKED (also `product-repo-lock-timeout`, same mutex as WORKTREE above — `detail.phase` is now one of `worktree` / `finish-sync` / `finish`, since FINISH itself acquires this mutex TWICE) — the pre-existing three (board move to `Done`, the closing issue comment, the final `git worktree remove`) still PARK the same `finish-failed` reason on a non-zero exit, `detail.step` naming which one (`board-move` / `issue-comment` / `worktree-remove`); this is deliberately the one park in the whole daemon that blocks on what is everywhere else a best-effort side effect (`board.js`'s own moveCard convention), because a card that cannot be marked `Done` is not done. Action B1.4 extends the SAME `finish-failed` vocabulary rather than inventing new reason strings, `detail.step` naming the new failure: `merge-sha-lookup` (`gh pr view` failed or returned no usable `mergeCommit.oid` — cannot safely determine anything past this point, always PARKS) · `bench-diff-check` (the `git diff --name-only` call itself failed — same reasoning) · `fast-forward` (the checkout was not on `main`, was dirty of TRACKED changes, or `git merge --ff-only` itself refused — `detail.reason` names which; PARKS only when `benchTouched` is true, i.e. reinstalling would have been necessary and unsafe; when `benchTouched` is false the card's PR has already merged and this is journalled as `main-fast-forward-failed`, not parked — a fast-forward failure this row's own drift measurement already showed real and non-blocking must not stall the whole backlog over a merge that never touched the bench; R3, post-verification third pass: `detail.reason` is `check-failed` — never `wrong-branch`/`dirty` — when the BRANCH or STATUS probe command itself failed to run (`detail.check` names which, `detail.exit` its real exit code) rather than genuinely answering \"wrong branch\"/\"dirty\", so a maintainer reading the journal is not misled into hunting for uncommitted work that was never there) · `bench-dir-unreadable` (R2/W2, post-verification third pass: `config.spoBenchDir`'s own `spool`/`running` could not be read for a reason OTHER than \"the directory simply is not there\" — `detail.code` carries the real errno; thrown immediately, on the FIRST read, never after polling, since no amount of waiting turns a misconfigured or permission-denied directory readable) · `bench-reinstall` (`bash scripts/bench-install.sh` itself exited non-zero). A stuck reinstall — `spawnStep`'s SINGLE attempt timing out, never retried (R2, post-verification third pass: matches `npm-gate`'s own pre-existing exemption, for the identical reason — a killed `bash` can leave `npm run build:e2e`/`systemctl restart` still running underneath it, and a retry would build into the SAME `dist/` concurrently) — is instead `bench-install-timed-out`, the `${commandClass}-timed-out` family's newest member (`command-timeout.js`'s own `'bench-install'` class, `SPO_TIMEOUT_BENCH_INSTALL_MS`, default 15 minutes, `detail.retried: false`). Every outcome of the new preamble is journalled either way (`merge-sha-lookup-failed` -- `gh pr view` exited non-zero or returned no usable `mergeCommit.oid`, so nothing past this point can be determined safely and the card parks `finish-failed`/`merge-sha-lookup`; `bench-diff-check-failed` -- the `git diff --name-only <mergeSha>^ <mergeSha>` call itself exited non-zero, so `benchTouched` is unknowable and the card parks `finish-failed`/`bench-diff-check`; both carry `prNumber` and the real `exit` code, and `bench-diff-check-failed` also carries the `mergeSha` step 2 resolved -- `main-fast-forwarded` / `main-fast-forward-failed`, `bench-busy-wait` per poll while the bench drains, `bench-idle` / `bench-idle-wait-timed-out`, `bench-reinstall-deferred` (R1, once the bound is exhausted with the bench still busy — see above), `bench-dir-unreadable`, `bench-reinstalled` / `bench-reinstall-failed` / `bench-reinstall-skipped`, `bench-diff-checked`), so the journal alone answers \"did the worker get reinstalled, was a reinstall ever deferred, and is one owed right now\" without reading a log. |" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "test/helpers.js:65-94", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "function isolatedEnv() {", last: "}" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "worker.ts:779-780", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  process.on('SIGTERM', () => process.exit(0));", last: "  process.on('SIGINT', () => process.exit(0));" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "scripts/finish.sh:245-247", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "if git -C \"$MAIN_REPO\" diff --name-only \"${merge_sha}^\" \"$merge_sha\" | grep -qE '^src/e2e/bench/|^scripts/bench-'; then", last: "  bash \"$MAIN_REPO/scripts/bench-install.sh\"" },
  { file: "doc/bench-audit-2026-09-02.md", citation: "scripted.js:1944-1996", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "async function realFinish(ctx, deps = {}) {", last: "}" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "scripts/finish.sh:245-247", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "if git -C \"$MAIN_REPO\" diff --name-only \"${merge_sha}^\" \"$merge_sha\" | grep -qE '^src/e2e/bench/|^scripts/bench-'; then", last: "  bash \"$MAIN_REPO/scripts/bench-install.sh\"" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "cli.ts:88", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "const KNOWN_FLAGS = new Set(['type', 'wait', 'timeout-min', 'lease-minutes', 'ref']);" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "doc/state-machine-spec.md:207", at: "HEAD", first: "| FINISH | script | Action B1.4: FINISH now actually keeps the promise this row always made — fast-forward the main product checkout (`config.productRepo`), then reap the worktree, then close the task (board sync: Done + short comment). Before this action it never did: `realFinish` only ever did the board move, the issue comment and the worktree remove, so a PR merged by the daemon left `config.productRepo` exactly as stale as one merged by a human who never ran `npm run finish` (SPO-WebClient's own script) — measured root cause of the bench worker silently running a stale binary for 3.5 days across 11 merges (a commit changed both halves of a flag contract atomically and correctly; the bench worker, installed once from a binary and never rebuilt, disagreed with the new job body from the moment it landed). Two NEW steps run first, inside their own product-repo-lock critical section (phase `finish-sync`, ahead of the pre-existing teardown phase `finish` below — see product-repo-lock.js/product-repo-hold.js), in this order: (1) `git fetch origin`, then this card's own merge commit by PR number (`gh pr view <prNumber> --json mergeCommit` — ctx carries no merge sha directly; MERGE only ever enqueues and awaits the merge, never reads the resulting commit back), then `git diff --name-only <mergeSha>^ <mergeSha>` to learn whether the merge touched the bench worker's own sources (paths under `src/e2e/bench/` or `scripts/bench-`, the same test `scripts/finish.sh` already runs by hand) — this determines `benchTouched` REGARDLESS of whether the fast-forward below succeeds; (2) the fast-forward itself — refuse (never force) unless `config.productRepo` is on `main`, clean of TRACKED changes (`git status --porcelain --untracked-files=no` empty — post-verification hazard fix: narrowed from bare `--porcelain`, which counted untracked files and so refused in cases `scripts/finish.sh`'s own `git pull --ff-only` would sail straight through, parking a bench-touching card on a stray editor backup or scratch file in this human-shared checkout), and `git merge --ff-only origin/main` itself succeeds; (3) ONLY once the fast-forward succeeded AND `benchTouched` is true, wait for the bench worker to go IDLE — a second post-verification hazard fix: `bash scripts/bench-install.sh` ends in an unconditional `systemctl --user restart`, and this daemon runs `SPO_WORKERS=2` in production, so reinstalling while the bench is still busy can cut a SIBLING card's in-flight GATE mid-job (the cut job recovers as `INTERRUPTED`, writes no `verdicts/<sha>.json`, and that sibling parks `gate-non-attesting` — transient-retryable, but a REAL re-run of WORKTREE through GATE, not merely a wasted gate). `waitForBenchIdle` polls `~/.spo-bench/spool` and `~/.spo-bench/running` (the same two directories `spo status` already reports) until both are empty, bounded by `config.benchIdleWaitMaxPolls` × `config.benchIdleWaitPollIntervalMs` (default 180 × 5s = 15 minutes), reading `config.spoBenchDir` itself, never a hardcoded path (R2/W2, post-verification third pass: an UNREADABLE spool/running — anything other than \"the directory simply is not there\" — is never silently read as idle; it PARKS immediately, `finish-failed`/`bench-idle-wait`/`bench-dir-unreadable`, distinguishably from a merely busy bench, the same \"tell a misconfiguration apart from a genuine empty answer\" pattern `realGate`'s own `verdictDirExists` already uses one function away). R1 (post-verification third pass): a bench that stays BUSY for the whole bound no longer PARKS — it DEFERS. The old park was wrong on three counts, all measured: `finish-failed` is not on `state-machine.js`'s `TRANSIENT_RETRY_REASONS` (terminal, human-only); the park fired BEFORE the board move below, so a card whose PR had ALREADY MERGED sat in `Merging` with its worktree still on disk; and the 15-minute bound is not generous against SPO-WebClient's own bench leases — `worker.ts`'s own `DEFAULT_LEASE_MINUTES = 30` / `MAX_LEASE_MINUTES = 120` constants mean an ORDINARY human bench lease (2×–8× the bound) would terminally park any bench-touching card the daemon finished during it. So the bound being exhausted now journals `bench-reinstall-deferred` (`detail`/event carry `mergeSha`, `prNumber`, the last-observed `spool`/`running`, and the attempt count), records the debt DURABLY (`journal.js`'s `writeBenchReinstallOwed`, `<journalRoot>/bench-reinstall-owed.json` — survives a daemon restart, and a SECOND bench-touching card deferring during the same busy window overwrites this ONE record with its own, newer `mergeSha` rather than accumulating a duplicate — `bash scripts/bench-install.sh` always rebuilds from whatever is CURRENTLY checked out, so only the latest sha is ever useful to retry with), and lets FINISH complete NORMALLY — board move, comment, worktree remove, `DONE` — exactly as if nothing were owed. Round 4: the debt is paid back by the NEXT card's own WORKTREE, from inside that card's own product-repo lock span (`payBenchReinstallDebtIfOwed`, see the WORKTREE row above for the full mechanism) rather than a separate daemon scan timer — round 3's `orchestrator/bench-reconcile.js` shipped exactly that timer and was deleted: it held the SAME product-repo lock from a THIRD process the mutex's own wait-bound derivation (`product-repo-lock.js`'s `waitBoundMs`) assumes cannot exist. The journal alone must always answer \"is a reinstall owed right now\" — every branch above is named and loud on purpose; (4) only once the bench is confirmed idle, `bash scripts/bench-install.sh` (the same script `scripts/finish.sh`'s human-session rule runs) reinstalls the worker — never run against a checkout that could not be verified fresh first, which would install the wrong binary and report success, reproducing the exact defect this action closes, and never run while the bench is still busy, which would reproduce the SAME defect from the other direction (a cut sibling job). | DONE | PARKED (also `product-repo-lock-timeout`, same mutex as WORKTREE above — `detail.phase` is now one of `worktree` / `finish-sync` / `finish`, since FINISH itself acquires this mutex TWICE) — the pre-existing three (board move to `Done`, the closing issue comment, the final `git worktree remove`) still PARK the same `finish-failed` reason on a non-zero exit, `detail.step` naming which one (`board-move` / `issue-comment` / `worktree-remove`); this is deliberately the one park in the whole daemon that blocks on what is everywhere else a best-effort side effect (`board.js`'s own moveCard convention), because a card that cannot be marked `Done` is not done. Action B1.4 extends the SAME `finish-failed` vocabulary rather than inventing new reason strings, `detail.step` naming the new failure: `merge-sha-lookup` (`gh pr view` failed or returned no usable `mergeCommit.oid` — cannot safely determine anything past this point, always PARKS) · `bench-diff-check` (the `git diff --name-only` call itself failed — same reasoning) · `fast-forward` (the checkout was not on `main`, was dirty of TRACKED changes, or `git merge --ff-only` itself refused — `detail.reason` names which; PARKS only when `benchTouched` is true, i.e. reinstalling would have been necessary and unsafe; when `benchTouched` is false the card's PR has already merged and this is journalled as `main-fast-forward-failed`, not parked — a fast-forward failure this row's own drift measurement already showed real and non-blocking must not stall the whole backlog over a merge that never touched the bench; R3, post-verification third pass: `detail.reason` is `check-failed` — never `wrong-branch`/`dirty` — when the BRANCH or STATUS probe command itself failed to run (`detail.check` names which, `detail.exit` its real exit code) rather than genuinely answering \"wrong branch\"/\"dirty\", so a maintainer reading the journal is not misled into hunting for uncommitted work that was never there) · `bench-dir-unreadable` (R2/W2, post-verification third pass: `config.spoBenchDir`'s own `spool`/`running` could not be read for a reason OTHER than \"the directory simply is not there\" — `detail.code` carries the real errno; thrown immediately, on the FIRST read, never after polling, since no amount of waiting turns a misconfigured or permission-denied directory readable) · `bench-reinstall` (`bash scripts/bench-install.sh` itself exited non-zero). A stuck reinstall — `spawnStep`'s SINGLE attempt timing out, never retried (R2, post-verification third pass: matches `npm-gate`'s own pre-existing exemption, for the identical reason — a killed `bash` can leave `npm run build:e2e`/`systemctl restart` still running underneath it, and a retry would build into the SAME `dist/` concurrently) — is instead `bench-install-timed-out`, the `${commandClass}-timed-out` family's newest member (`command-timeout.js`'s own `'bench-install'` class, `SPO_TIMEOUT_BENCH_INSTALL_MS`, default 15 minutes, `detail.retried: false`). Every outcome of the new preamble is journalled either way (`merge-sha-lookup-failed` -- `gh pr view` exited non-zero or returned no usable `mergeCommit.oid`, so nothing past this point can be determined safely and the card parks `finish-failed`/`merge-sha-lookup`; `bench-diff-check-failed` -- the `git diff --name-only <mergeSha>^ <mergeSha>` call itself exited non-zero, so `benchTouched` is unknowable and the card parks `finish-failed`/`bench-diff-check`; both carry `prNumber` and the real `exit` code, and `bench-diff-check-failed` also carries the `mergeSha` step 2 resolved -- `main-fast-forwarded` / `main-fast-forward-failed`, `bench-busy-wait` per poll while the bench drains, `bench-idle` / `bench-idle-wait-timed-out`, `bench-reinstall-deferred` (R1, once the bound is exhausted with the bench still busy — see above), `bench-dir-unreadable`, `bench-reinstalled` / `bench-reinstall-failed` / `bench-reinstall-skipped`, `bench-diff-checked`), so the journal alone answers \"did the worker get reinstalled, was a reinstall ever deferred, and is one owed right now\" without reading a log. |" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "scripts/nightly-check.sh:70-73", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  ENVIRONMENT|INTERRUPTED)", last: "    ;;" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "orchestrator/steps/scripted.js:292-293", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  const nightly = readJsonSafe(path.join(config.spoBenchDir, 'nightly', 'latest.json'));", last: "  if (nightly && nightly.verdict === 'FAIL' && nightly.sha === originMainSha) {" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "src/e2e/config.ts:93", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  minIntervalMinutes: numberFromEnv('E2E_MIN_INTERVAL_MINUTES', 0)," },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "worker.ts:302", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    const head = request.fingerprint.head;" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "finish.sh:275-276", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "    mkdir -p \"$SESSIONS_DIR\"", last: "    printf '%s\\t%s\\t%s\\n' \"$here\" \"$branch\" \"$(git -C \"$here\" rev-parse HEAD)\" > \"$SESSIONS_DIR/$(session_key \"$here\").finished\"" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "board-take.sh:109-110", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  finished_marker=\"$(session_marker finished 2>/dev/null || true)\"", last: "  if [ -n \"$finished_marker\" ] && [ -f \"$finished_marker\" ]; then" },
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "bin/spo:1283", at: "HEAD", first: "    const data = collectAll(sources);" }, // same re-pin, same reason, card #219 -- see the sibling doc's own entry above
  { file: "doc/bench-plan-derived-2026-09-02.md", citation: "test/helpers.js:65-94", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "function isolatedEnv() {", last: "}" },
];

const LIVE_RANGE_PINS = [
  { file: "doc/state-machine-spec.md", citation: "dispatcher.js:635-648", at: "HEAD", first: "    if (childrenSignalled && outcome === 'crashed') {", last: "      return;" },
  { file: "doc/state-machine-spec.md", citation: "intake.js:953-955", at: "HEAD", first: "// it. First, availability: fable/high wedged the whole report pipeline for 12.8 hours on", last: "// every one dying on \"You've reached your Fable 5 limit\" (api_error_status=429). At the time," },
  { file: "orchestrator/README.md", citation: "intake.js:953-955", at: "HEAD", first: "// it. First, availability: fable/high wedged the whole report pipeline for 12.8 hours on", last: "// every one dying on \"You've reached your Fable 5 limit\" (api_error_status=429). At the time," },
  { file: "orchestrator/README.md", citation: "lock.js:354-385", at: "HEAD", first: "  // CREATE-AND-PUBLISH MUST BE ATOMIC (verification of action 6.3; the defect this closes was", last: "  // accountStateLockWaitMs bound governs." }, // re-pinned from :278-309, then :330-361 -- card #219's fix pass added the future-guard and an expanded LINUX_CLK_TCK comment (24 more lines) above this range, a true pure shift; content byte-identical at :354-385
  { file: "orchestrator/dispatcher.js", citation: "daemon.js:665-666", at: "HEAD", first: "  process.once('exit', () => {", last: "    if (dispatcherHandle) dispatcherHandle.killAllChildren('SIGTERM');" },
  { file: "orchestrator/journal.js", citation: "auto-pull.js:58-66", at: "HEAD", first: "// computeAutoPullBudget below reads `queued` BEFORE `inFlight`, not incidentally: dispatcher.js's", last: "// Reversing the read order is therefore not cosmetic." },
  { file: "orchestrator/orphan-scan.js", citation: "auto-pull.js:58-66", at: "HEAD", first: "// computeAutoPullBudget below reads `queued` BEFORE `inFlight`, not incidentally: dispatcher.js's", last: "// Reversing the read order is therefore not cosmetic." },
  { file: "orchestrator/state-machine.js", citation: "auto-pull.js:58-66", at: "HEAD", first: "// computeAutoPullBudget below reads `queued` BEFORE `inFlight`, not incidentally: dispatcher.js's", last: "// Reversing the read order is therefore not cosmetic." },
  { file: "orchestrator/state-machine.js", citation: "auto-pull.js:58-66", at: "HEAD", first: "// computeAutoPullBudget below reads `queued` BEFORE `inFlight`, not incidentally: dispatcher.js's", last: "// Reversing the read order is therefore not cosmetic." },
  { file: "orchestrator/steps/llm.js", citation: "intake.js:953-955", at: "HEAD", first: "// it. First, availability: fable/high wedged the whole report pipeline for 12.8 hours on", last: "// every one dying on \"You've reached your Fable 5 limit\" (api_error_status=429). At the time," },
  { file: "scripts/usage-report.js", citation: "orchestrator/token-recovery.js:10-18", at: "HEAD", first: "// The fix is not a second accounting path: `claude` itself writes every call's usage into the", last: "// scripts/usage-report.js once did, until SPO-Pipeline#170 made them share one reader." },
  { file: "orchestrator/park-loop.js", citation: "intake.js:953-955", at: "HEAD", first: "// it. First, availability: fable/high wedged the whole report pipeline for 12.8 hours on", last: "// every one dying on \"You've reached your Fable 5 limit\" (api_error_status=429). At the time," },
  { file: "orchestrator/README.md", citation: "dispatcher.js:635-648", at: "HEAD", first: "    if (childrenSignalled && outcome === 'crashed') {", last: "      return;" },
  { file: "orchestrator/README.md", citation: "SPO-WebClient/.claude/settings.json:109-127", at: "935283890fa0593c5c5d0b41cceeaec2c1972c6f", first: "  \"hooks\": {", last: "            \"timeout\": 10" },
];

const BLUNT_PINS = [
  { file: "orchestrator/README.md", citation: "doc/state-machine-spec.md:200", at: "HEAD", first: "| CHECK | script | invariant substring check first (action 1.8: `orchestrator/invariants.js` re-resolves the PLAN-time baseline against the worktree as it now stands — an id that resolved at PLAN and no longer does is the one regression this fails on; one PLAN itself could never resolve was already excluded from the baseline and can never fail here; a missing/unparsable invariants file is journalled, never a failure), pure `fs`, no spawn, run before the three subprocess checks below so a free check never waits behind three that cost a spawn each; then typecheck, lint, `coverage:changed` (≥ 93 % on new/modified lines) | PUSH_PR | DIAGNOSE |" },
  { file: "orchestrator/park-loop.js", citation: "doc/remediation-progress.md:664", at: "HEAD", first: "- **DIAGNOSE surfacing**: 6 tasks entered DIAGNOSE, 18 attempts total, 4 of them ending in a park." },
  { file: "orchestrator/state-machine.js", citation: "park-loop.js:1341", at: "HEAD", first: "    if (state.state !== 'PARKED') continue;" },
];

// CCA_PINS -- action 11.2 (#206): doc/comment-corpus-audit-2026-09-03.md's own file-tied
// citations, pinned the same way BENCH_PINS pins the two bench docs above -- this is a THIRD
// dated record (its own header names the SPO-Pipeline commit it measured, 7902164, and cross-
// checks against `~/SPO-WebClient` at d03ea8b7), previously excluded from CORPUS_FILES outright
// ("written AFTER the corpus it measured") with NO content check on any of its 37 file-tied
// citations -- issue #206's own comments measured 5 of them already stale or re-pinned to
// today's tree by hand with nothing failing (bin/spo:2220/:1139, and three more that drifted
// silently). All 37 citations here name SPO-Pipeline paths (never a product `.ts`/product-repo
// file), so all 37 are frozen at 7902164309c1766d7b785daab9ba94ff6472bc1d, the commit this doc's
// own header names for this repo -- never d03ea8b7 (that commit is named for `~/SPO-WebClient`,
// and nothing in this doc cites a product file by line).
//
// Four citations were re-pinned by hand to the WRONG commit before this action, matching today's
// tree instead of the doc's own measurement commit, by FOUR separate, unrelated commits (`git
// log --follow -p -- doc/comment-corpus-audit-2026-09-03.md`, verified by hand -- not assumed from
// this data file's own prior comment, which got the attribution, one count, and one of the four
// facts wrong; see this action's own fix-pass report for what was wrong and how it was checked):
//   - both `bin/spo` citations, twice each: `:1838`->`:2189` and `:993`->`:1109` (card #208,
//     a885996), then `:2189`->`:2220` and `:1109`->`:1139` (card #214, b232de5) -- re-pins to match the file
//     as it grew, on the day each card landed. Corrected here to `:1838`/`:993` respectively,
//     verified against `git show 7902164:bin/spo`:
//       - `bin/spo:1838` is `  if (cmd === 'tokens') return cmdTokens(opts);`, the line the doc's
//         own prose quotes verbatim; at 7902164 the file is only 1899 lines, so `:2220` was never
//         even in bounds there.
//       - `bin/spo:993` is `function cmdCostDeprecated(opts) {`; at 7902164, `:1139` is
//         `function cmdAccounts(opts) {`, an unrelated function.
//   - `CLAUDE.md:29` -> `:35` (commit 769eac5, 2026-09-05, an UNRELATED CLAUDE.md restructure --
//     not #208/#214, and not about this doc at all). Corrected here to `:29`: at 7902164,
//     `CLAUDE.md:29` is `- **Verdict by exit code**, never by reading \`gh\`'s text output.`, the
//     bullet the doc's prose quotes; CLAUDE.md grew 6 lines above it between 7902164 and 769eac5,
//     so `:35` only ever became correct once that growth landed, never at the doc's own
//     measurement commit.
//   - `orchestrator/config.js:704` -> `:779` (also commit 769eac5) -> `:839` (commit 7932e5d) --
//     two separate moves, both unrelated to #208/#214, tracking the same comment as config.js grew
//     above it. Corrected here to `:704`: at 7902164, `:704` is
//     `  // is 125 lines of \`gh api graphql\` -- resolve the option id, write, re-read to confirm -- with`,
//     the comment line the doc's prose quotes verbatim. `:839` at 7902164 is CODE, not a comment --
//     the `remoteReportQueueCeiling` ternary -- so `:839` was never a comment at any commit this
//     citation could honestly claim; and by HEAD the real comment has moved on again, to `:1028`,
//     so `:839` is not even correct today.
// All four corrections were also applied to the doc's own prose (doc/comment-corpus-audit-2026-
// 09-03.md) in the same change, per this action's own "undo the re-pins" instruction.
//
// `README.md:34`/`:35`/`:37` name the bare basename `README.md`, ambiguous under
// resolveCitationTarget (4 tracked files share it: the repo root, `orchestrator/README.md`,
// `prompts/README.md`, and a test fixture). Fix pass 11.2 (D1, driver decision): a consistent
// wrong re-pin of these three (allowlisted rather than pinned, in this action's own first pass)
// shipped green 74/74 when probed (`:37` -> `:38`, doc text and allowlist entry moved together) --
// an allowlisted citation is never content-checked at all, so it caught nothing. Fixed by adding
// an optional `path` field to a pin (test/citation-pins.js's resolvePins), honoured only when the
// cited bare name is genuinely ambiguous, `path`'s own basename matches the cited name, and `path`
// contains a "/" (so it resolves directly, never re-entering the ambiguous-basename search a bare
// `path: 'README.md'` would fall straight back into). All three are pinned below with
// `path: "./README.md"`, verified true at 7902164: root README.md:34/35/37 are respectively the
// `bin/spo` subcommand table row, the node-test invocation table row, and the `bench/` promise
// table row this doc's prose describes. CCA_ALLOWLIST is retired -- every one of the doc's 37
// file-tied citations now has a real pin, none left unpinnable.
//
// `doc/board-audit.md:20` and `orchestrator/README.md:1062` are each cited TWICE at different
// points in the doc's own prose (two different findings resting on the same fact) -- CCA_PINS
// carries each of those citations twice too, to match the doc's own citation COUNT, the same
// array-equality (not set-equality) discipline BENCH_PINS's own cross-check test already uses.
const CCA_PINS = [
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/park-loop.js:755", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  const journal = (event, detail) => appendEvent(taskDir, 'PARKED', event, detail);" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/park-loop.js:825", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "        if (exit === 0) journal('abandon-branch-deleted', { branch, sha: localSha });" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "test/doc-constant-sweep.test.js:352", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  assert.ok(checked >= 8, `expected several file:line citations, found ${checked} -- has the citation style changed?`);" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/config.js:489", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  // orchestrator/lock.js) -- see doc/daemon-crash-recovery.md for the incident this covers" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/jewels-inventory.md:14", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "| Bench worker + L2 live gate + nightly + verdicts | `scripts/bench-*`, `src/e2e/bench/`, systemd unit, `~/.spo-bench/` | **migrates → SPO-Pipeline `bench/`** | The pre-production environment; the only proof a patch works against the real world |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "bin/spo:1838", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  if (cmd === 'tokens') return cmdTokens(opts);" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/park-loop.js:179", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "// machine.js's buildCtx resets it to 0 on every retry) -- reading the journal instead is what" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "bin/spo:407-408", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "// actually governs when the daemon will pick this card back up (state-machine.js's", last: "// isEligibleNow reads `notBefore` off the task, not off the journal) -- the two carry the same" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:1062", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  single missing event is why **14 of 18 tasks' journals stop at `Merging` while the board reads" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/park-loop.js:925", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "// of 18 tasks, and the BOARD is already right -- because the project has the built-in \"Item" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/board-audit.md:20", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "(`PVTSSF_lADOEyAVD84BhYwkzhgUrL0`) currently holds all 12 options — the 7 old-pipeline names" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "bin/spo:715", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "      // is simply waiting for the next drain (state-machine.js's isEligibleNow would take it" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/state-machine.js:1564", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "function isQueueEntryEligibleNow(task, nowMs) {" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/park-loop.js:219", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "// ---- action 5.1d: surface DIAGNOSE on the card -------------------------------------------------" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:790", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "`doc/state-machine-spec.md:98` has always promised CHECK runs an \"invariant substring check\", and" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/state-machine-spec.md:98", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "   auto-retry path above still journals `transient-retry` (`{reason, attempt, delayMs," },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/state-machine-spec.md:121", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "| CHECK | script | invariant substring check first (action 1.8: `orchestrator/invariants.js` re-resolves the PLAN-time baseline against the worktree as it now stands — an id that resolved at PLAN and no longer does is the one regression this fails on; one PLAN itself could never resolve was already excluded from the baseline and can never fail here; a missing/unparsable invariants file is journalled, never a failure), pure `fs`, no spawn, run before the three subprocess checks below so a free check never waits behind three that cost a spawn each; then typecheck, lint, `coverage:changed` (≥ 93 % on new/modified lines) | PUSH_PR | DIAGNOSE |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:2371", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "node --test --test-timeout=30000 test/*.test.js" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "bin/spo:1654", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "// (CLAUDE.md: \"Verdict by exit code, never by reading text output\") -- 0 only when the run" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:2056", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "**Exit code is the verdict** (CLAUDE.md: \"Verdict by exit code, never by reading text output\") —" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "CLAUDE.md:29", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "- **Verdict by exit code**, never by reading `gh`'s text output." },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/board-audit.md:20", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "(`PVTSSF_lADOEyAVD84BhYwkzhgUrL0`) currently holds all 12 options — the 7 old-pipeline names" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "scripts/daemon-install.sh:103", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "echo \"== journals: $REPO/journal/   status: bin/spo status   cost: bin/spo cost\"" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "bin/spo:993", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "function cmdCostDeprecated(opts) {" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/permissions.md:114-169", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "```json", last: "```" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/config.js:704", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  // is 125 lines of `gh api graphql` -- resolve the option id, write, re-read to confirm -- with" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:1062", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "  single missing event is why **14 of 18 tasks' journals stop at `Merging` while the board reads" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "orchestrator/README.md:1180", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "3 of 18 tasks, and in all three the issue was already closed:" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/setup.md:15", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "cd ~/SPO-Deploy && ./deploy.sh setup dev" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "console/prod-version.js:13", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "//                   (SPO-WebClient's own tag -> release -> SPO-Deploy's deploy.sh flow," },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/state-machine-spec.md:9", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "**What in this document (and the rest of the doc/prompts/orchestrator-comment corpus) is" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/state-machine-spec.md:117", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "| INTAKE | script | take next task file from `queue/` (priority = file order; sources: board export, `/triage-report`, later in-game reports). **Action 3.2:** for a `kind: \"card\"` task, the criterion and title are scanned for a protected-file mention (`.claude/settings.json`, `.claude/settings.local.json`, anything under `.claude/hooks/` — see `orchestrator/intake.js`'s `detectProtectedFiles`) after the `invalid-task-json`, `shadow.forceState`, and `real-flag-required` checks but before `appendEvent('INTAKE', 'ok')` runs; a hit parks the task at zero cost, since INTAKE never makes an LLM call either way. | WORKTREE | PARKED (also `plan-requires-protected-files`, `{source: 'criterion', matches}`, when the card's own criterion or title names a protected file) |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/state-machine-spec.md:445", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "| `npm-run` | 660s (11 min) | every other `npm run <alias>` (`typecheck`, `lint`, `coverage:changed`, `board:take`, `board:move`, `pr:wait`) — bounded below by `pr:wait`'s own internal 600s bound (`scripts/pr-wait.sh`: 20 polls × 30s), so a legitimate \"still in the merge queue\" `pr:wait` exit is never killed by this timeout first |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "doc/environments.md:32", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", first: "`doc/E2E-POLICY.md`, `doc/bench-worker.md`, `doc/E2E-TESTING.md`." },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "README.md:34", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", path: "./README.md", first: "| `bin/spo` | Console CLI (`status` / `task <id>` / `parked` / `resume <id>` read only the journals; `account add` / `account enable`/`disable` / `account clear-cooldown` / `dashboard` write local state; `ask` / `pull` / `intake` / `triage --file` / `recette` write to real GitHub, see `orchestrator/intake.js` and `orchestrator/README.md` § Intake) | ✅ v0 |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "README.md:35", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", path: "./README.md", first: "| `test/` | `node --test test/*.test.js` suite for the orchestrator, shadow mode, temp queue/journal dirs (never bare — see `orchestrator/README.md` § Tests) | ✅ |" },
  { file: "doc/comment-corpus-audit-2026-09-03.md", citation: "README.md:37", at: "7902164309c1766d7b785daab9ba94ff6472bc1d", path: "./README.md", first: "| `bench/` | Bench worker, L2 live gate, nightly, replay | planned — migrated from `SPO-WebClient` (`scripts/bench-*`, `src/e2e/bench/`) |" },
];

module.exports = { BENCH_PINS, LIVE_RANGE_PINS, BLUNT_PINS, CCA_PINS };
